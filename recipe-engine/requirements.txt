# MealVista Recipe Engine (database + optional Gemini)

# API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# Gemini API (for recipe variety in search)
google-generativeai>=0.3.0

# Groq API (free tier fallback when Gemini quota exceeded – runtime recipes, no local load)
groq>=0.4.0

# HTTP
requests>=2.31.0
